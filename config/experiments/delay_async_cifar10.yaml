# @package _global_
defaults:
  - override /runtime: async
  - override /model: simple_cnn
  - _self_

dataset:
  workload: cifar10

client:
  num_cpus: 2
  num_gpus: 0.2
  local_epochs: 3
  simulate_delay: true

async:
  total_train_time: 120
  waiting_interval: 10

wb:
  mode: online
  project: autofl-async
