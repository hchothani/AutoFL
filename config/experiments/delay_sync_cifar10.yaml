defaults:
  - ../config

model:
  name: simple_cnn
  num_classes: 10
  pretrained: false

dataset:
  workload: cifar10

server:
  num_clients: 5
  num_rounds: 10
  fraction_fit: 1.0
  fraction_eval: 1.0
  min_fit: 5
  min_eval: 5
  strategy: fedavg

client:
  num_cpus: 2
  num_gpus: 0.2
  local_epochs: 2
  batch_size: 32
  learning_rate: 0.1
  simulate_delay: true
  
async:
  enabled: false

wb:
  enabled: true
  name: sync_cifar10_test
  project: autofl-sync
