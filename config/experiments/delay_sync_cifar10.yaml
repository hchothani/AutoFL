# @package _global_
defaults:
  - override /runtime: sync
  - override /model: simple_cnn
  - _self_

dataset:
  workload: cifar10

client:
  num_cpus: 2
  num_gpus: 0.2
  local_epochs: 2
  batch_size: 32
  learning_rate: 0.1
  simulate_delay: true

wb:
  mode: online
  project: autofl-sync
