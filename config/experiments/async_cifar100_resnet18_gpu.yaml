# @package _global_
defaults:
  - override /runtime: async
  - override /model: resnet18
  - _self_

dataset:
  workload: cifar100

async:
  total_train_time: 1800
  waiting_interval: 30

wb:
  mode: online
  project: autofl-async
