# @package _global_
defaults:
  - override /runtime: sync
  - override /model: simple_cnn
  - _self_

dataset:
  workload: cifar10

server:
  num_clients: 5
  num_rounds: 10
  fraction_fit: 1.0
  fraction_eval: 1.0
  min_fit: 5
  min_eval: 5
  strategy: fedavg

client:
  num_cpus: 2
  num_gpus: 0.2
  local_epochs: 2
  batch_size: 32
  learning_rate: 0.1

wb:
  mode: online
  project: autofl-sync
