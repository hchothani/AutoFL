# AutoFL Experiment: MNIST + MobileNetV2
model:
  name: mobilenet_v2

dataset:
  workload: mnist
  split: niid

server:
  num_clients: 5

cl:
  enabled: true
  num_experiences: 5

async:
  enabled: true
  total_train_time: 900
  waiting_interval: 10

context:
  enabled: true
  threshold: 0.20  # Sweep: [0.15, 0.20, 0.25, 0.30]

lora:
  enabled: true

wb:
  group: cosine_experiments
  run_name_keys:
    - "dataset.workload"
    - "model.name"
    - "context.threshold"
    - "async.enabled"
    - "context.enabled"
    - "lora.enabled"
    - "cl.enabled"
