# AutoFL Experiment: SVHN + SimpleCNN
model:
  name: simple_cnn

dataset:
  workload: svhn
  split: niid

server:
  num_clients: 5

cl:
  enabled: true
  num_experiences: 5

async:
  enabled: true
  total_train_time: 900
  waiting_interval: 10

context:
  enabled: true
  threshold: auto  # Dynamically computed via universal statistical regression

lora:
  enabled: true

wb:
  group: cosine_experiments
  run_name_keys:
    - "dataset.workload"
    - "model.name"
    - "context.threshold"
    - "async.enabled"
    - "context.enabled"
    - "lora.enabled"
    - "cl.enabled"
