# AutoFL Experiment: MNIST + SimpleCNN
model:
  name: simple_cnn

dataset:
  workload: mnist
  split: niid

server:
  num_clients: 5

cl:
  enabled: true
  num_experiences: 5

async:
  enabled: true
  total_train_time: 900
  waiting_interval: 10

context:
  enabled: true
  threshold: 0.35  # Sweep: [0.20, 0.30, 0.40, 0.50]

lora:
  enabled: true
