# AutoFL Experiment: EuroSAT + SimpleCNN
model:
  name: simple_cnn

dataset:
  workload: eurosat
  split: niid

server:
  num_clients: 5

cl:
  enabled: true
  num_experiences: 5

async:
  enabled: true
  total_train_time: 900
  waiting_interval: 10

context:
  enabled: true
  threshold: 0.35  # Sweep: [0.20, 0.30, 0.35, 0.40, 0.50] 00.35 seems best by a long shot. same was calculated using the formula

lora:
  enabled: true

wb:
  group: cosine_experiments
  run_name_keys:
    - "dataset.workload"
    - "model.name"
    - "context.threshold"
    - "async.enabled"
    - "context.enabled"
    - "lora.enabled"
    - "cl.enabled"
