# @package _global_
defaults:
  - override /runtime: async
  - override /model: simple_cnn
  - _self_

dataset:
  workload: cifar10

server:
  num_clients: 5
  num_rounds: 10
  fraction_fit: 1.0
  fraction_eval: 1.0
  min_fit: 5
  min_eval: 5
  strategy: fedavg

client:
  num_cpus: 2
  num_gpus: 0.2
  local_epochs: 3

async:
  total_train_time: 120
  waiting_interval: 10

wb:
  mode: online
  project: autofl-async
